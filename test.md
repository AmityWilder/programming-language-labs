# Where is the test folder?

Because of how the Rust programming language is structured, tests are in the [test module](./src/test). Tests are part of the source code itself, not off in a separate framework.
