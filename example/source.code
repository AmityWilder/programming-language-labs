def foo#5 {}

{ } } } // unbalanced brackets outside of a string are a visualized as an error, but won't be acknowledged until parsing

[ } ] ]

fn main() {
    for i in 0..5 {
        let ch = 'n';
        let ch = '\n';
        let float = 1.6343e24;
        let float1 = 1.6343e-24;
        const FOO_BAR = 3.1415;
        let x = `some text that has \"escapes\" but not \"expressions\"`;
        my'text = "this \"text\" is \x1b[91mred\x1b[0m";
        let interpolated = `apples: \x64 ${i} + \x1b 1 = ${i + 1234invalid * 3 { { foo } * { bar } /*
            interpolated strings can have inner braces as long as they're balanced */ } &&
            // the only rule is no nested interpolated strings
            "see?" + ' ' + "you can nest strings, even with \"escapes\", including \` and they'll be highlighted!"
        } to escape an expression, just put a backslash (\\\${}) like usual`; // TODO: that part isn't working yet!
        let my'text /* there is absolutely no reason for the apostrophe,
            /* i just /* wanted */ to /* show /* it */ off */ */ */ = "hello world";
            "aaaaa\x
              aaa" + 1
        print(my'text); // output the contents of `my'text` to the console
    }
}
